{
  "name": "reelance",
  "version": "1.0.0",
  "description": "Reelance — Hire the Cut. Skip the Chaos.",
  "main": "index.html",
  "scripts": {
    "dev": "vite --port 3000 --open",
    "start": "vite --port 3000 --open",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
